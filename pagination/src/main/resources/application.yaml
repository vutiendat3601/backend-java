server:
  port: 8080
spring:
  application:
    name: pagination
  # datasource:
  #   url: jdbc:h2:mem:test
  #   username: sa
  #   password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        "[format_sql]": true
  # h2:
  #   console:
  #     enabled: true
  #     path: /h2-console

springdoc:
  api-docs:
    path: /v1/openapi
  swagger-ui:
    path: /v1/openapi.html
